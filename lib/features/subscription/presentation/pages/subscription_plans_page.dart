import 'package:flutter/widgets.dart';

class SubscriptionPlansPage extends StatelessWidget {
  const SubscriptionPlansPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Text('Subscription Plans Page');
  }
}
